import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  Root: {
    alignItems: 'stretch',
    flex: 1,
    flexDirection: 'row',
    height: '100%',
    justifyContent: 'flex-start',
    paddingTop: 20,
  },
});
